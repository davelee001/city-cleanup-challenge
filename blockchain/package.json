{
  "name": "celo-integration",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:alfajores": "npx hardhat run --network alfajores scripts/deploy.js",
    "deploy:local": "npx hardhat run --network local scripts/deploy.js",
    "contractkit:example": "node scripts/contractkit_example.js"
  },
  "devDependencies": {
    "hardhat": "^2.17.0",
    "@nomiclabs/hardhat-ethers": "^2.1.0",
    "ethers": "^6.0.0"
  },
  "dependencies": {
    "@celo/contractkit": "^1.10.1",
    "dotenv": "^16.0.0"
  }
}
