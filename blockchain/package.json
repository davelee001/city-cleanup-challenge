{
  "name": "celo-integration",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:celo-sepolia": "npx hardhat run --network celoSepolia scripts/deploy-reward-treasury.js",
    "deploy:local": "npx hardhat run --network local scripts/deploy-reward-treasury.js"
  },
  "devDependencies": {
    "hardhat": "^2.17.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "chai": "^4.5.0",
    "dotenv": "^16.6.1",
    "ethers": "^6.13.0"
  }
}
