export declare class Lock {
    private locked;
    private emitter;
    constructor();
    get listenersCount(): number;
    tryAcquire(): boolean;
    acquire(): Promise<void>;
    release(): void;
}
