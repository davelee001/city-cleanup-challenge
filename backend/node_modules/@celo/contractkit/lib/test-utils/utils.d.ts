import { StableToken } from '@celo/base';
import BigNumber from 'bignumber.js';
import { ContractKit } from '../kit';
export declare const startAndFinishEpochProcess: (kit: ContractKit) => Promise<void>;
export declare const topUpWithToken: (kit: ContractKit, stableToken: StableToken, recipientAddress: string, amount: BigNumber) => Promise<void>;
