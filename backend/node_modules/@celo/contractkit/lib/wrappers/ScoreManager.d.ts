import { ScoreManager } from '@celo/abis/web3/ScoreManager';
import { BaseWrapper } from './BaseWrapper';
/**
 * Contract handling validator scores.
 */
export declare class ScoreManagerWrapper extends BaseWrapper<ScoreManager> {
    getGroupScore: (group: string) => Promise<import("bignumber.js").default>;
    getValidatorScore: (validator: string) => Promise<import("bignumber.js").default>;
}
export type ScoreManagerWrapperType = ScoreManagerWrapper;
